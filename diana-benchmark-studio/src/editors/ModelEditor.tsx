import { ModelTable } from "../controls/ModelTable";
import { useStudio } from "../workspace/studio-context";
export function ModelEditor() { const { spec, updateSpec } = useStudio(); return <section className="editor-section" aria-labelledby="model-title"><header><p className="eyebrow">05 / Models</p><h1 id="model-title">Choose a compact classical suite</h1><p>Every functional control maps to trusted generated code. The first version deliberately limits custom programs to three estimators.</p></header>{spec.mode === "hormonbench_preset" && spec.official ? <div className="table-scroll"><table className="config-table"><caption>Frozen Hormonbench model identities</caption><thead><tr><th>Identifier</th><th>Role</th></tr></thead><tbody>{spec.official.baseline_ids.map((id) => <tr key={id}><th scope="row">{id}</th><td>Classical baseline</td></tr>)}<tr><th scope="row">{spec.official.reference_model}</th><td>Reference model — not a fourth baseline</td></tr></tbody></table></div> : <ModelTable spec={spec} onChange={(models) => updateSpec((draft) => { draft.models = models; })} />}</section>; }
