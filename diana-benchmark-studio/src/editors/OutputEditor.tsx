import { NumericField } from "../controls/NumericField";
import { useStudio } from "../workspace/studio-context";
export function OutputEditor() { const { spec, updateSpec } = useStudio(); const official = spec.mode === "hormonbench_preset"; return <section className="editor-section" aria-labelledby="output-title"><header><p className="eyebrow">07 / Review & runtime</p><h1 id="output-title">Prepare a reproducible local run</h1><p>Aggregate outputs are public-safe by design. Optional row predictions remain private and governed.</p></header><NumericField id="runtime-seed" label="Runtime seed" value={spec.runtime.seed} min={0} max={2_147_483_647} disabled={official} onChange={(value) => updateSpec((draft) => { draft.runtime.seed = value; })} /><NumericField id="runtime-workers" label="Workers" value={spec.runtime.workers} min={1} max={32} disabled={official} onChange={(value) => updateSpec((draft) => { draft.runtime.workers = value; })} /><label className="field-row"><span>Notebook execution mode</span><select value={spec.runtime.execution_mode} disabled={official} onChange={(event) => updateSpec((draft) => { draft.runtime.execution_mode = event.currentTarget.value as "synthetic" | "local_data"; })}><option value="synthetic">Synthetic fixture</option><option value="local_data">Local governed data</option></select></label>{!official && <label className="field-row"><span>Results directory</span><input value={spec.output.directory} aria-label="Results directory" onChange={(event) => updateSpec((draft) => { draft.output.directory = event.currentTarget.value; })} /></label>}{official && spec.official && <><label className="field-row"><span>Verified runner mode</span><select value={spec.official.run_mode} onChange={(event) => updateSpec((draft) => { if (draft.official) draft.official.run_mode = event.currentTarget.value as "preflight" | "synthetic" | "full"; })}><option value="preflight">Preflight / verify only</option><option value="synthetic">Synthetic smoke</option><option value="full">Full licensed-data run</option></select></label><div className="contract-lines" aria-label="Frozen official runtime contract"><p><strong>Repository root</strong><code>{spec.official.repo_env}</code></p><p><strong>Licensed data root</strong><code>{spec.official.data_env}</code></p><p><strong>Output isolation</strong><span>diana_hormonbench_runs/&lt;fingerprint&gt;</span></p><p><strong>Execution</strong><span>Sequential folds · CatBoost 4 threads · NumPy/CPU H3P</span></p><p><strong>Reporting</strong><span>Public aggregate evidence; participant rows remain private</span></p></div></>}<details className="advanced-settings"><summary>Advanced output & privacy settings</summary><label className="switch-row"><input type="checkbox" checked={spec.output.save_predictions} disabled={official} onChange={(event) => updateSpec((draft) => { draft.output.save_predictions = event.currentTarget.checked; draft.output.aggregate_only = !event.currentTarget.checked; })} />Save private row predictions locally</label><p>Row predictions remain on the local governed system and are excluded from public aggregate output.</p></details><div className="output-tree"><code>{spec.output.directory}/</code><code>├─ metrics.json</code><code>├─ leaderboard.csv</code><code>└─ run_manifest.json</code></div></section>; }
