import type { TaskSpec } from "../contracts/task-spec";
export function SplitDiagram({ spec }: { spec: TaskSpec }) { const train = 1 - spec.split.validation_fraction - spec.split.test_fraction; return <figure className="protocol-figure"><figcaption>Split contract</figcaption><div className="split-bars"><span style={{ flex: train }}>Train<br />{(train * 100).toFixed(0)}%</span><span style={{ flex: spec.split.validation_fraction }}>Validate<br />{(spec.split.validation_fraction * 100).toFixed(0)}%</span><span style={{ flex: spec.split.test_fraction }}>Test<br />{(spec.split.test_fraction * 100).toFixed(0)}%</span></div><p>{spec.split.type.replaceAll("_", " ")} · seed {spec.split.seed}</p></figure>; }
