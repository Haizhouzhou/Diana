import { Database, Target, Split, SlidersHorizontal, Boxes, Gauge, FileCheck2 } from "lucide-react";
import { ValidationIndicator } from "../controls/ValidationIndicator";
import { useStudio, type StageId } from "./studio-context";
const stages: { id: StageId; label: string; icon: typeof Database }[] = [{ id: "data", label: "Data", icon: Database }, { id: "task", label: "Task", icon: Target }, { id: "split", label: "Split", icon: Split }, { id: "preprocessing", label: "Preprocessing", icon: SlidersHorizontal }, { id: "models", label: "Models", icon: Boxes }, { id: "metrics", label: "Metrics", icon: Gauge }, { id: "review", label: "Review", icon: FileCheck2 }];
export function ProtocolRail() { const { stage, setStage, lint } = useStudio(); return <nav className="protocol-rail" aria-label="Protocol stages"><ol>{stages.map(({ id, label, icon: Icon }, index) => { const relevant = [...lint.blocking, ...lint.warnings].filter((issue) => issue.stage === id); const state = relevant.some((issue) => issue.severity === "blocking") ? "error" : relevant.length ? "warning" : "complete"; return <li key={id}><button type="button" className={stage === id ? "active" : ""} aria-current={stage === id ? "step" : undefined} onClick={() => setStage(id)}><span className="stage-number">{String(index + 1).padStart(2, "0")}</span><Icon className="stage-icon" size={15} aria-hidden="true" /><span className="stage-label">{label}</span><ValidationIndicator state={state} /></button></li>; })}</ol></nav>; }
